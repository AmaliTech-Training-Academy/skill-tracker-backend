package com.amalitech.user.service.model.enums;

/**
 * Represents the status of task generation for a user's skill.
 * Used to track the lifecycle of task creation requests.
 */
public enum TaskGenerationStatus {
    PENDING,
    COMPLETED,
    FAILED
}
